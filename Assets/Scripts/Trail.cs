using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class Trail : MonoBehaviour
{
    

    private RPGControllerNew

    // Start is called before the first frame update
    void Start()
    {
        TrailX = new int[Size];
        TrailY = new int[Size];
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
*/